const guidelines = {
  logo: "Logo must be horizontal orientation. Logo must have clear space around it (minimum 40px). Text must be readable. No distortion or pixelation. Must use official brand colors only.",
  banner: "Banner must be standard web dimensions (728x90 or 300x250). Must include company logo. Must be high resolution. No pixelation or distortion. Text must be readable at normal viewing distance.",
  social: "Image must be square (1080x1080) or standard social media dimensions. Must include branding elements. Text overlay must be readable. Colors must match brand guidelines. No low-quality or blurry images.",
  print: "Resolution must be 300 DPI minimum. Must be CMYK color mode. Must include bleed area. All text must be embedded or outlined. No RGB colors allowed.",
};

export default guidelines;
